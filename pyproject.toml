[project]
name = "blog"
version = "0.1.0"
description = "A presonal blog to just put my ideas in the wild!"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "mkdocs-material>=9.7.4",
]
